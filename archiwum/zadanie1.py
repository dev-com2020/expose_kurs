text = "Matematyka jest królową wszystkich nauk"

licznik = 0

for znak in text:
    if znak == 'a':
        licznik += 1 #alternatywa dla licznik = licznik + 1


print("Litera 'a' wystąpiła:",licznik,"razy")
